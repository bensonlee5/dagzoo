seed: 19

dataset:
  task: classification
  missing_rate: 0.25
  missing_mechanism: mnar
  missing_mnar_logit_scale: 1.6

runtime:
  device: auto

output:
  out_dir: data/run_missingness_mnar

diagnostics:
  enabled: false

steering:
  enabled: false
